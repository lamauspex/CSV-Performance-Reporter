# CSV Performance Reporter

Скрипт для анализа производительности сотрудников на основе данных из CSV файлов.

## Описание

Этот инструмент позволяет анализировать данные о сотрудниках из CSV файлов и генерировать отчеты по их эффективности. Скрипт поддерживает обработку нескольких файлов одновременно и имеет расширяемую архитектуру для добавления новых типов отчетов.

## Возможности

- Анализ производительности сотрудников по позициям
- Поддержка множественных CSV файлов
- Валидация данных и обработка ошибок
- Форматированный вывод в виде таблиц
- Расширяемая архитектура для новых отчетов

## Установка

1. Клонируйте репозиторий
2. Установите зависимости:
```bash
pip install -r requirements.txt
```

## Использование

### Базовое использование

```bash
python main.py --files data/employees1.csv --report performance
```

### Работа с несколькими файлами

```bash
python main.py --files data/employees1.csv data/employees2.csv --report performance
```

### Параметры

- `--files`: Пути к CSV файлам с данными (обязательный параметр)
- `--report`: Название отчета для генерации (обязательный параметр)

### Поддерживаемые отчеты

- `performance` - отчет по эффективности сотрудников (средняя эффективность по позициям)

## Формат входных данных

CSV файлы должны содержать следующие колонки:

- `name` - имя сотрудника
- `position` - должность
- `completed_tasks` - количество выполненных задач
- `performance` - оценка эффективности (от 0 до 5)
- `skills` - навыки (через запятую)
- `team` - команда
- `experience_years` - опыт работы в годах

## Архитектура и расширение

Проект построен с использованием паттерна Strategy и имеет расширяемую архитектуру:

- `CSVProcessor` - обработка и валидация CSV файлов
- `ReportGenerator` - фабрика отчетов
- `BaseReport` - базовый класс для всех отчетов
- `PerformanceReport` - конкретная реализация отчета по эффективности



## Тестирование

Запуск тестов:

```bash
pytest tests/ -v
```

Покрытие тестами:

```bash
pytest tests/ --cov=. --cov-report=html
```

## Пример вывода

```
╔════╤══════════════════════╤═════════════════════╤═══════════════════════════╗
║ №  │ Позиция              │Средняя эффективность│ Количество сотрудников    ║
╠════╪══════════════════════╪═════════════════════╪═══════════════════════════╣
║ 1  │ Backend Developer    │ 4.67                │ 3                         ║
║ 2  │ Mobile Developer     │ 4.63                │ 3                         ║
║ 3  │ DevOps Engineer      │ 4.73                │ 2                         ║
║ 4  │ Data Engineer        │ 4.80                │ 2                         ║
╚════╧══════════════════════╧═════════════════════╧═══════════════════════════╝
```

## Обработка ошибок

Скрипт корректно обрабатывает следующие ситуации:
- Отсутствующие файлы
- Некорректная структура CSV
- Недопустимые значения в данных
- Неподдерживаемые типы отчетов